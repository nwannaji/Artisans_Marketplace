#include "Generated.xcconfig"

// Google Maps API key — for development builds, read from .env file.
// In CI/CD, set GOOGLE_MAPS_API_KEY as an environment variable.
// SECURITY: Never commit real API keys to source control.
GOOGLE_MAPS_API_KEY=$(GOOGLE_MAPS_API_KEY)